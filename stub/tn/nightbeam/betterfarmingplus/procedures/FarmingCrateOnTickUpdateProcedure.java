package tn.nightbeam.betterfarmingplus.procedures;

import net.minecraft.world.level.LevelAccessor;

/**
 * COMPILE-ONLY STUB of the class from the base mod "better_farming_plus".
 * <p>
 * This mirrors only the signature the Mixin annotation processor needs to
 * resolve the injection target. It is compiled against but NOT bundled into the
 * addon jar; at runtime the real class from the base mod is used instead.
 */
public class FarmingCrateOnTickUpdateProcedure {
	public static void execute(LevelAccessor world, double x, double y, double z) {
	}
}
